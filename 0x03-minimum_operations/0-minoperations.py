#!/usr/bin/python3

"""

"""

def minOperations(n):
	op = 0
	    x = 2
	    while n > 1:
	        while n % x == 0:
	            op += i
	            n = int(n / x)
	        x += 1
	    return op_count
